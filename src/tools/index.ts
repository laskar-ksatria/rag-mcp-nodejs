import { env } from "../env.js";

// ======================================================================================================== //
// SAVE_RAG
// ======================================================================================================== //
export const save_to_rag = async ({ text }: { text: string }) => {
  try {
    // init_pinecone
  } catch (error) {}
};
